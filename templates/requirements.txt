requests==2.25.1  # HTTP requests library
flask==2.0.1  # Web framework, if needed for hosting your app
graphviz==0.16  # For diagram generation
pillow==8.3.1  # For image processing
notion==0.0.28  # Notion API client (example package, verify actual package name and version)
